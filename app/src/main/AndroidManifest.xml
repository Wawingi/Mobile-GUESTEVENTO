<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wawingisebastiao.gestevento">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:theme="@style/AppTheme.NoActionBar"
            android:name=".MainActivity">
        </activity>
        <activity android:name=".RegistarUtilizador"></activity>
        <activity android:name=".ListarUtilizador"></activity>
        <activity android:name=".Notificacao"></activity>
        <activity android:name=".RegistarEvento"></activity>
        <activity android:name=".Login"></activity>
    </application>

</manifest>